name: CI

on:
  push:
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build Code
      run: ./build.ps1
      shell: pwsh
    - name: Prepare Testing Action
      run: |
        (Get-Content -Path ./.github/workflows/testing-action/action.yml -Raw) -replace '$$CUSTOM_ACTION_REPO$$',$env:GITHUB_REPO -replace '$$GITHUB_SHA$$',$ENV:GITHUB_SHA | Set-Content -Path ./.github/workflows/testing-action/action.yml
      shell: pwsh
    - name: Debug Stuff
      run: Get-Content -path ./.github/workflows/testing-action/action.yml
      shell: pwsh
    - name: Run Action
      uses: ./.github/workflows/testing-action
